<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Genres</title>
</head>
<body>
    <h1>Genres</h1>
    <table border="1">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <% genres.forEach(genre => { %>
                <tr>
                    <td><%= genre._id %></td>
                    <td><%= genre.name %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>
